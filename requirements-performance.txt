# Performance testing dependencies for Luna Collection
psutil>=5.9.0
pytest>=7.0.0
pytest-cov>=4.1.0
numpy>=1.24.0
torch>=2.0.0  # For GPU memory monitoring
pytest-xdist>=3.5.0  # For parallel test execution
# Note: pytest-benchmark removed due to compatibility issues
# pygal and pygaljs removed as they're not needed for our simplified tests